{% extends 'base.html.twig' %}

{% block title %}Instant Zen{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center text-center mt-5">L'équipe Instant Zen</h1>
    <div class="container-fluid d-flex justify-content-center mt-2 text-center">
        {% for teacher in teachers %}
            <div class="col-md-3 d-flex flex-column mt-5">
                <div class="card text-center m-3 bg-white shadow">
                    <div class="card-header p-3 bg-white p-4 shadow">
                        <img class="img-fluid d-block mb-3 mx-auto" src="{{ teacher.url }}" width="400" height="200" alt="Card image cap">
                        <h5 class="card-title">{{ teacher.name}}</h5>
                    </div>
                    <div class="card-body p-3 container-tertiary">
                        <p class="card-text text-dark">Description : {{ teacher.description }}</p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
